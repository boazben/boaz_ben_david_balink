import styled from 'styled-components';

export const StyledIcon = styled.div`
    font-size: ${({size}) => size};
    cursor: ${({cursor}) => cursor};
    border: none;
   

    &:hover {
        
    }
`